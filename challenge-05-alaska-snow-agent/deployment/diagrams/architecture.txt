Alaska Department of Snow - System Architecture
================================================

┌──────────────────────────────────────────────────────────────────────────┐
│                         👤 USER INTERFACE                                │
│                                                                          │
│                          Web Browser                                     │
│                               │                                          │
│                               ▼                                          │
└───────────────────────────────┬──────────────────────────────────────────┘
                                │
                                │ 1. User Query
                                │
┌───────────────────────────────▼──────────────────────────────────────────┐
│                      ☁️  CLOUD RUN (Streamlit)                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    🛡️  SECURITY LAYER                            │   │
│  │                                                                   │   │
│  │  Input Sanitization ──► Model Armor ──► Output Sanitization     │   │
│  │  (Prompt Injection)     (PII Filter)     (Response Validation)   │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                               │                                          │
│                               ▼                                          │
│                      AlaskaSnowAgentEnhanced                            │
│                                                                          │
└──────────────┬─────────────────┬─────────────────┬─────────────────────┘
               │                 │                 │
               │ 2. Embed        │ 3. Retrieve     │ 4. Generate
               │    Query        │    Context      │    Response
               ▼                 ▼                 ▼
    ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
    │  🤖 VERTEX AI    │  │  📊 BIGQUERY     │  │  🤖 VERTEX AI    │
    │                  │  │                  │  │                  │
    │ text-embedding-  │  │  snow_vectors    │  │ Gemini 2.5 Flash │
    │     004          │  │  (Vector Index)  │  │  (Generator)     │
    │                  │  │                  │  │                  │
    │  • 768-dim       │  │  • 50 FAQs       │  │  • RAG Prompt    │
    │  • Embeddings    │  │  • Top-k=3       │  │  • Context +     │
    │                  │  │  • Similarity    │  │    Query         │
    └──────────────────┘  └──────────────────┘  └──────────────────┘


DATA FLOW:
─────────

 1. User submits query via browser
 2. Input sanitization (Model Armor - Prompt Injection & Jailbreak Detection)
 3. Query embedded using text-embedding-004
 4. Vector search finds top-3 relevant FAQs from BigQuery
 5. Context + query sent to Gemini 2.5 Flash
 6. Response generated using RAG pattern
 7. Output sanitization (Model Armor - PII Detection)
 8. Clean response displayed to user
 9. Interaction logged to BigQuery (audit trail)


KEY COMPONENTS:
──────────────

┌─────────────────────────────────────────────────────────────────────┐
│ 🔒 MODEL ARMOR (Security)                                           │
│  • Prompt Injection Detection (LOW_AND_ABOVE confidence)            │
│  • Jailbreak Detection                                              │
│  • Malicious URI Filtering                                          │
│  • PII/SDP (Sensitive Data Protection)                              │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ 📊 BIGQUERY (Data Layer)                                            │
│  • snow_faqs_raw: Source FAQ data (50 rows)                         │
│  • snow_vectors: Vector embeddings + metadata                       │
│  • interaction_logs: Audit trail (timestamp, query, response)       │
│  • embedding_model: Remote ML model for text-embedding-004          │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ 🤖 VERTEX AI (AI Processing)                                        │
│  • Gemini 2.5 Flash: Response generation                            │
│  • text-embedding-004: Query/document embeddings                    │
│  • EvalTask: Quality metrics (groundedness, fluency, coherence)     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ ☁️  CLOUD RUN (Hosting)                                             │
│  • Serverless Streamlit deployment                                  │
│  • Auto-scaling based on traffic                                    │
│  • Environment variables: PROJECT_ID, REGION                        │
└─────────────────────────────────────────────────────────────────────┘


SECURITY ARCHITECTURE:
─────────────────────

  User Input
      │
      ▼
  ┌───────────────────┐
  │ Input Validation  │ ──► Block malicious queries
  └───────────────────┘
      │
      ▼
  ┌───────────────────┐
  │ RAG Processing    │ ──► Ground in factual data
  └───────────────────┘
      │
      ▼
  ┌───────────────────┐
  │ Output Validation │ ──► Redact PII from responses
  └───────────────────┘
      │
      ▼
  Clean Response


REQUIREMENTS MAPPING:
────────────────────

Req #1: Backend data store for RAG          → BigQuery vector search ✅
Req #2: Access to backend API functionality → Geocoding + Weather APIs ✅
Req #3: Unit tests for agent functionality  → 12+ pytest tests ✅
Req #4: Evaluation using Google service     → Vertex AI EvalTask ✅
Req #5: Prompt filtering & validation       → Model Armor ✅
Req #6: Log all prompts and responses       → BigQuery logging ✅
Req #7: Generative AI agent on website      → Streamlit on Cloud Run ✅

Target Score: 39-40/40 points (97-100%)
